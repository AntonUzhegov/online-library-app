version: '3.8'

services:
  mysql:
    build: .
    container_name: library-mysql
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: library
